var gameModel = require("Model");

cc.Class({
    extends: cc.Component,

    properties: {
        SkinType:{
            default:null,
            type:cc.Sprite
        },
    },

    // LIFE-CYCLE CALLBACKS:

    onLoad () {
    },

    start () {
        
    },

    // update (dt) {},
});
